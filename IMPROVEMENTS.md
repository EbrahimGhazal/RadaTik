# اقتراحات تحسين وتطوير تطبيق RadaTik

## 1. مزامنة MikroTik التلقائية (تم تنفيذها)

تم تنفيذ آلية مزامنة تلقائية بين قاعدة البيانات وسيرفرات MikroTik باستخدام:
- **طابور داخلي (Channel):** `IMikroTikSyncQueue` لتغذية مهام المزامنة فوراً
- **اعتراض SaveChanges:** `MikroTikSaveChangesInterceptor` يكتشف تغييرات `Client` و`Profile` ويضيفها للطابور
- **خدمة خلفية:** `MikroTikSyncBackgroundService` تستهلك الطابور وتنفذ مكالمات MikroTik بأسرع وقت

**ما يتم مزامنته تلقائياً:**
- إضافة/تحديث/حذف **Client** → AddPPPoEUser / UpdatePPPoEUser / DeletePPPoEUser
- إضافة/تحديث/حذف **Profile** → AddProfileToMikroTik / UpdateProfileInMikroTik / DeleteProfileFromMikroTik

**ملاحظة:** إذا كانت الواجهة تستدعي MikroTik مباشرة قبل SaveChanges، فإن المزامنة التلقائية ستتعامل مع حالة "موجود مسبقاً" كنجاح (idempotent) دون تكرار أخطاء.

---

## 2. ما تم إصلاحه سابقاً

- **بوابة العميل (ClientPortal):** عند عدم وجود عميل مرتبط بحساب المستخدم، كان التوجيه إلى `Home/Index` يسبب حلقة إعادة توجيه (Client → clientPortal/dashboard → ClientPortal/Index → Home → clientPortal...). تم تغيير التوجيه إلى `Account/Profile` مع رسالة توضيحية.
- **SystemAdminController:** إزالة تحذيرات nullable باستخدام متغير محلي `activeTab` بدلاً من `ViewData["ActiveTab"].ToString()` في الشروط.

---

## 3. الأداء (Performance)

- **تخزين مؤقت للإحصائيات:** لوحة تحكم مدير النظام ومدير الشركة تقوم بعدة استعلامات عند كل طلب. يمكن استخدام `IMemoryCache` أو `IDistributedCache` مع انتهاء صلاحية قصير (مثلاً 1–5 دقائق) لتقليل الحمل على قاعدة البيانات.
- **استعلامات N+1:** مراجعة أي قوائم تعرض علاقات (مثل العملاء مع القطاع/المستقبل) واستخدام `.Include()` أو التحميل المسبق لتجنب استعلام إضافي لكل صف.
- **MikroTik:** استدعاءات API قد تكون بطيئة؛ يمكن تنفيذ تفعيل/تعطيل الحساب في الخلفية (مثلاً عبر `IHostedService` أو queue) مع إرجاع "قيد المعالجة" للمستخدم، ثم إشعار عند الانتهاء إن أمكن.

---

## 4. الأمان (Security)

- **تأكيد الصلاحيات حسب السياق:** التأكد في كل action أن العميل/الشبكة/نقطة التحصيل تتبع المستخدم أو شبكته (مثلاً عدم السماح بوصول عميل شبكة أخرى عبر تغيير معرف في الرابط).
- **معدل الطلبات (Rate limiting):** تفعيل rate limiting على تسجيل الدخول وواجهات العميل (طلب تغذية رصيد، تجديد) لتقليل الهجمات الآلية.
- **تسجيل الأحداث الحساسة:** تسجيل محاولات الدخول الفاشلة، تغيير كلمة المرور، وتفعيل/تعطيل العملاء في السجلات.

---

## 5. تجربة المستخدم (UX)

- **رسائل موحدة:** استخدام `TempData["Success"]` و `TempData["Error"]` و `TempData["Info"]` بشكل متسق في كل المناطق، وعرضها في Layout واحد (مثلاً شريط علوي أو toast).
- **تحميل الصفحات (Loading):** في الصفحات التي تستدعي MikroTik أو عمليات طويلة، إظهار مؤشر تحميل أو زر "جاري المعالجة" لتجنب النقر المتكرر.
- **تأكيد الإجراءات الحرجة:** تأكيد قبل تعطيل عميل أو حذف بيانات مهمة (مثلاً عبر modal أو صفحة تأكيد).

---

## 6. جودة الكود والصيانة

- **تحذيرات EF Core:** معالجة تحذيرات الـ migrations الحالية:
  - `RequestPriority` على `MaintenanceRequest`: إما استخدام nullable أو تعيين sentinel value.
  - `VATPercentage` و `ProfilePriceHistory`: تحديد `HasPrecision` أو `HasColumnType` لـ decimal لتجنب القص التلقائي.
- **Nullable reference types:** إكمال معالجة التحذيرات في المشروع (مثلاً في `MikroTikService` و `ClientPortalController`) لتحسين الأمان من NullReference.
- **اختبارات آلية:** إضافة unit tests للتحقق من التوجيه حسب الدور، واختبارات تكاملية للـ API الرئيسية (تسجيل دخول، تجديد اشتراك، إلخ).

---

## 7. البنية والبنية التحتية

- **الإعدادات الحساسة:** التأكد من أن connection strings وبيانات MikroTik و API keys مخزنة في User Secrets (تطوير) أو متغيرات بيئة/ Key Vault (إنتاج) وليست مضمّنة في الكود.
- **Health checks:** إضافة endpoints لفحص صحة التطبيق (قاعدة البيانات، وربما اتصال MikroTik) لاستخدامها في أوامر المراقبة والنشر.

---

## 8. الاختبار اليدوي الموصى به

- تسجيل الدخول بكل دور (مدير نظام، مدير شركة، موظف، نقطة تحصيل، عميل) والتحقق من التوجيه إلى المنطقة الصحيحة.
- محاولة الوصول إلى منطقة أخرى دون تسجيل خروج (يجب أن ينتهي إلى accessDenied أو إعادة توجيه مناسبة).
- عميل بدون سجل في جدول `Clients`: يجب أن يصل إلى صفحة الملف الشخصي مع رسالة الخطأ وليس حلقة إعادة توجيه.
- تجديد اشتراك من بوابة العميل (مع رصيد كافٍ) والتحقق من تحديث MikroTik وقاعدة البيانات.

---

*تم إنشاء هذا الملف كجزء من مراجعة الترابط بين الأدوار والمناطق وإصلاح الثغرات المعروفة.*
